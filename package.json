{
  "name": "ccc-arena",
  "version": "0.2.0",
  "description": "Run multiple coding agents (Claude Code, Codex CLI, ...) on the same task in isolated git worktrees and compare the results.",
  "type": "module",
  "license": "MIT",
  "author": "Shuichi Suzuki",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jacksuzuki/ccc-arena.git"
  },
  "homepage": "https://github.com/jacksuzuki/ccc-arena#readme",
  "bugs": {
    "url": "https://github.com/jacksuzuki/ccc-arena/issues"
  },
  "bin": {
    "arena": "dist/arena.js"
  },
  "files": [
    "dist",
    ".claude/skills",
    "README.md",
    "README.ja.md",
    "README.zh-CN.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "node src/arena.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test test/*.test.ts",
    "test:package": "npm run build && node scripts/test-package.mjs",
    "prepublishOnly": "npm run typecheck && npm test && npm run test:package",
    "check:dist": "npm run build && git diff --exit-code --stat dist"
  },
  "dependencies": {
    "yaml": "^2.9.1",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@types/node": "^22.19.0",
    "typescript": "^7.0.2"
  }
}
